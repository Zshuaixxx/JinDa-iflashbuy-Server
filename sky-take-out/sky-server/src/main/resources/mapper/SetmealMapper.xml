<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">
    <insert id="addSetmeal" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal (category_id,name,price,status,description,image,create_time,update_time,create_user,update_user,merchant_id)
        values (#{setmeal.categoryId},#{setmeal.name},#{setmeal.price},#{setmeal.status},#{setmeal.description},#{setmeal.image},#{setmeal.createTime},#{setmeal.updateTime},#{setmeal.createUser},#{setmeal.updateUser},#{merchantId})
    </insert>

    <select id="pageViewSetmeal" resultType="com.sky.vo.SetmealVO">
        select setmeal.*,category.name as categoryName from setmeal left outer join category on setmeal.category_id = category.id
        <where>
            <if test="setmealPageQueryDTO.name != null and setmealPageQueryDTO.name!=''">
                setmeal.name like concat('%',#{name},'%')
            </if>
            <if test="setmealPageQueryDTO.categoryId != null">
                and category_id=#{categoryId}
            </if>
            <if test="setmealPageQueryDTO.status != null">
                and setmeal.status=#{status}
            </if>
            and setmeal.merchant_id = #{merchantId}
        </where>
    </select>
    
    <select id="merchant_pageViewSetmeal" resultType="com.sky.vo.SetmealVO">
        select setmeal.*,category.name as categoryName from setmeal left outer join category on setmeal.category_id = category.id
        <where>
            <if test="setmealPageQueryDTO.name != null and setmealPageQueryDTO.name!=''">
                setmeal.name like concat('%',#{setmealPageQueryDTO.name},'%')
            </if>
            <if test="setmealPageQueryDTO.categoryId != null">
                and category_id=#{setmealPageQueryDTO.categoryId}
            </if>
            <if test="setmealPageQueryDTO.status != null">
                and setmeal.status=#{setmealPageQueryDTO.status}
            </if>
            and setmeal.merchant_id = #{merchantId}
        </where>
    </select>

    <select id="getSetmealById" resultType="com.sky.vo.SetmealVO">
        select setmeal.*,category.name as categoryName from setmeal left outer join category on setmeal.category_id = category.id
        where setmeal.id=#{id}
    </select>
    
    <select id="getSetmealByIdWithMerchantCheck" resultType="com.sky.vo.SetmealVO">
        select setmeal.*,category.name as categoryName from setmeal left outer join category on setmeal.category_id = category.id
        where setmeal.id=#{id} and setmeal.merchant_id=#{merchantId}
    </select>

    <update id="updateSetmeal" >
        update setmeal
        <set>
            <if test="categoryId != null">
                category_id=#{categoryId},
            </if>
            <if test="description != null and description!=''">
                description=#{description},
            </if>
            <if test="image != null and image != ''">
                image=#{image},
            </if>
            <if test="name != null and name != ''">
                name=#{name},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="status != null">
                status=#{status},
            </if>
            <if test="updateTime != null">
                update_time=#{updateTime},
            </if>
            <if test="updateUser != null">
                update_user=#{updateUser},
            </if>
            <if test="merchantId != null">
                merchant_id=#{merchantId},
            </if>
        </set>
        where id=#{id}
    </update>

    <select id="getDishBySetmealId" resultType="com.sky.entity.Dish">
        select dish.* from dish left outer join setmeal_dish on setmeal_dish.dish_id=dish.id
        where setmeal_dish.setmeal_id=#{id}
    </select>
</mapper>